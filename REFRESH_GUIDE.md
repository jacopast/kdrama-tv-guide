---
project: Playground
type: workflow
tags: [kdrama, refresh, workflow]
---

# 🔄 데이터 최신화 워크플로우

## 왜 이 문서가 있나요?
안티그래비티(로컬 Gemini 에이전트)는 실시간 웹 검색 없이 `dramas.json`을 채우다 보니, 이미 종영한 드라마나 실제로 존재하지 않는 편성 정보가 섞여 들어가는 문제가 있었습니다 (2026-08-19 확인·수정).

이 문제를 막으려면 **웹 검색이 가능한 에이전트(Claude 등)**가 정기적으로 아래 절차를 따라 `dramas.json`을 실제 소스로 검증·교체해야 합니다.

## 언제 실행하나요?
- `fetch_kdrama_schedule.py`를 실행하면 `dramas.json`이 **7일 넘게** 갱신되지 않았을 때 `K_DRAMA_TV_GUIDE.md` 하단에 경고 배너가 뜹니다. 그 배너를 보면 이 워크플로우를 실행하세요.
- 그 외에도 매주 한 번(예: 월요일) 습관적으로 돌리는 것을 권장합니다.

## Claude에게 그대로 붙여넣을 프롬프트

```
드라마 가이드(dramas.json)를 최신 데이터로 갱신해줘.

1. WebSearch로 오늘 날짜 기준 "이번 주 + 다음 주"(fetch_kdrama_schedule.py의 WEEKS_TO_SHOW 값만큼) 미국에서
   볼 수 있는 한국 드라마 편성을 Netflix, Rakuten Viki, KOCOWA+, Hulu/Disney+, Prime Video, Apple TV+
   6개 채널별로 조사해줘.
   - Soompi, ScreenRant, DramaWanderlust, 나무위키, 위키백과, 각 플랫폼 공식 페이지 등 실제 소스를 사용하고
   - 이미 종영했거나, 방영 예정 채널이 확인되지 않은 작품은 넣지 마.
2. 각 작품마다 실제 방영 요일/시간, 정확한 회차(방영 시작일 기준으로 역산 가능), 캐스팅, 정확한 스트리밍
   직링크(가짜 URL 금지 — 못 찾으면 플랫폼 검색/메인 페이지로 대체하고 그렇게 표시), 가능하면 닐슨코리아
   시청률(%)을 찾아줘.
3. 확인된 내용으로 dramas.json을 다시 작성해줘. 스키마는 요일 키가 아니라 **날짜 키**를 쓴다는 점에 주의:
   - 정규 편성: `"schedule": {"2026-08-24": "EP 07", "2026-08-25": "EP 08"}` 처럼 실제 방영 날짜(YYYY-MM-DD)를 키로.
   - 전편 공개(batch): `"isBatch": true, "releaseDate": "2026-08-28", "batchEp": "10부작 전편 공개 (8/28~)"`.
   - 각 항목에 `"verifiedAt": "오늘 날짜"` 남기기.
   - 실제로 방영 중인 채널이 없으면 억지로 채우지 말고 비워둬(정확성이 화려함보다 중요함).
   - 미국 플랫폼이 끝내 확인 안 되는 작품은 이 파일 하단 "확인 대기 중" 목록에 적어두고 dramas.json에는 넣지 마.
4. python3 fetch_kdrama_schedule.py 를 실행해서 K_DRAMA_TV_GUIDE.md와 kdrama_tv_guide.html을 동기화해줘.
5. README.md의 "변경 이력" 섹션에 오늘 날짜로 무엇을 갱신했는지 한 줄 남겨줘.
```

## 사람이 직접 확인할 때 체크리스트
- [ ] 각 드라마가 **실제로 이번 주에 방영/공개**되는지 (이미 끝난 작품 섞임 주의)
- [ ] 플랫폼 링크가 실제로 열리는지 (가짜 TMDB/Netflix ID 금지)
- [ ] 회차 번호가 방영 시작일 + 주기로 계산했을 때 말이 되는지
- [ ] `verifiedAt` 날짜가 오늘 날짜로 찍혀 있는지

## 구조적으로 바뀐 부분 (2026-08-19)
- `fetch_kdrama_schedule.py`가 더 이상 날짜를 하드코딩하지 않고, 실행 시점의 오늘 날짜 기준으로 주차를 자동 계산합니다 (`build_week(offset)`).
- `kdrama_tv_guide.html`도 페이지 로드 시 `updateDateHeaders()`가 헤더의 날짜 라벨을 US 동부시간 기준으로 다시 계산합니다.
- `dramas.json`이 7일 넘게 안 바뀌면 마크다운 하단에 경고 배너가 자동으로 뜹니다.

## 구조적으로 바뀐 부분 (2026-08-19, 2차 — 이번 주 + 다음 주)
- **스키마 변경**: 드라마별 `airDays`/`dayEpisodes`(요일 키)를 `schedule`(날짜 키, 예: `"2026-08-24": "EP 07"`)로 바꿨습니다. 요일만으로는 "이번 주 월요일"과 "다음 주 월요일"을 구분할 수 없어서, 정확한 캘린더 날짜를 직접 키로 씁니다. 몇 주를 보여주고 싶은지는 `fetch_kdrama_schedule.py`의 `WEEKS_TO_SHOW` 값만 바꾸면 됩니다(기본 2).
- 전편 공개(batch) 작품은 `isBatch`+`releaseDate`로 표시하고, 공개일이 지난 주에는 자동으로 Batch 컬럼에 계속 나타납니다.
- `kdrama_tv_guide.html`에 "📅 이번 주 / ▶ 다음 주" 탭이 생겼습니다. 탭을 누르면 헤더 날짜·그리드·통계가 모두 그 주 기준으로 다시 계산됩니다.
- `add_drama.py`로 새 드라마를 추가하면 선택한 요일 카테고리(월화/수목/금토/주말/OTT배치)를 기반으로 실제 `schedule`/`isBatch` 필드가 만들어집니다. 다만 구체적인 회차 날짜·번호는 비어있으니, 정확한 걸 원하면 REFRESH_GUIDE 프롬프트로 웹 검색 기반 검증을 한 번 더 돌리세요.

## 확인 대기 중 (플랫폼 미확정이라 편성표에서 제외됨)
- **신병4: 사보타주** (ENA, 8/24 첫방, 12부작) — 미국 스트리밍 플랫폼 미확인 (Kocowa 추정이나 미검증)
- **포핸즈** (tvN, 8/29 첫방, 12부작, 송강·이준영·장규리) — 미국 스트리밍 플랫폼 미확인 (Viki 추정이나 미검증)

다음 리서치 때 위 두 작품의 실제 미국 플랫폼이 확인되면 `dramas.json`에 추가해 주세요.
